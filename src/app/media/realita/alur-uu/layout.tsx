import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Alur Pembentukan Undang-Undang | Media Realita PPKn",
  description: "Simulasi interaktif langkah demi langkah proses pembentukan RUU hingga disahkan menjadi Undang-Undang.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
